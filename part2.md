# Phần 2 — Viết lại từ đầu (45 phút)

Đây là RULES.md #2 áp dụng ở quy mô lớn hơn: viết được lại từ đầu = hiểu thật.

## Việc cần làm

- [ ] Tạo một thư mục trắng, **không copy gì** từ `Pokemon-game`
- [ ] Viết lại `api.ts` và `game.ts` của game Pokemon từ file trắng, đúng chức năng
      như bản đang chạy trên `main`
- [ ] `tsc` chạy không lỗi
- [ ] Ghi lại chỗ nào phải mở docs để tra, và chỗ nào phải đoán

## Điều kiện

Trong 45 phút này **không mở** repo `Pokemon-game`, không mở lịch sử git của nó,
không mở tab cũ. Được mở docs TypeScript/MDN.

Không cần `user-interface.ts` và không cần chạy được game — chỉ cần hai file trên và
`tsc` sạch.

## Cách nộp

Comment kèm nội dung 2 file đã viết, output `tsc`, và phần ghi lại chỗ phải tra docs
hoặc phải đoán.

Viết không xong cũng nộp, ghi rõ dừng ở đâu và vướng gì. Phần ghi lại chỗ phải đoán
được tính điểm — nó cho biết chỗ nào chưa vững, và đó là thông tin có giá trị hơn một
file hoàn chỉnh nhờ nhớ máy móc.
